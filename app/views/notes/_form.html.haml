= form_with model: @note, local: true do |f|
  - if note.errors.any?
    .SettingGroupForm__errors
      %h2= "#{note.errors.full_messages.count}件のエラーが発生しました。"
      %ul
        - note.errors.full_messages.each do |message|
          %li= message
  = f.text_field :name, class: "SettingGroupForm__input", placeholder: "ノート名を入力してください"
  = f.submit "Save", class: "SettingGroupForm__button"